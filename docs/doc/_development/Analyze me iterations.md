# AnalyzeMe — Iteration Plan

> Детальный план разработки модуля AnalyzeMe.
> Колонки: Итерация | Задача | Цель | Ожидаемый результат | Статус

---

## Статусы
- Done
- In Progress
- Not Started

---

## Предварительный шаг — перенос из ForgeMe

| Итерация | Задача | Цель | Ожидаемый результат | Статус |
|---|---|---|---|--------|
| A.0 | Перенести `AnalyzeSection.tsx` из `modules/forge-me/` в `modules/analyze-me/`. Убрать импорт из ForgeMe `index.tsx`. Удалить `/forge-me/analyze` вызов с фронта. | Очистить ForgeMe от логики анализа. ForgeMe — только генератор. | ForgeMe работает без AnalyzeSection. Файл лежит в `modules/analyze-me/`. Бэкенд эндпоинт `/forge-me/analyze` пока остаётся нетронутым. | Done   |
| A.1 | Создать манифест `registry/analyze-me.ts`. Зарегистрировать в `registry/index.ts`. Добавить stub `index.tsx` в `modules/analyze-me/`. | AnalyzeMe появляется как модуль в системе — роут `/analyze-me` работает, карточка видна в MarketMe. | Переход на `/analyze-me` открывает страницу-заглушку. Модуль виден в MarketMe. | Done   |

---

## Итерация 1 — Скелет и архитектура

| Итерация | Задача | Цель | Ожидаемый результат | Статус |
|---|---|---|---|---|
| 1.1 | Создать `backend/services/analyze_me/` с `pyproject.toml` и пустым `router.py`. Добавить в uv workspace. | Изолированный backend-сервис по аналогии с forge_me. Ядро не знает о модуле до подключения. | Бэкенд стартует без ошибок. `GET /analyze-me/health` возвращает `{"status": "ok"}`. | Done |
| 1.2 | Подключить роутер в `core/main.py` через `try/except ImportError`. | Следовать принципу архитектуры: сервис подключается без изменения ядра. | `GET /analyze-me/health` работает в Swagger. | Done |
| 1.3 | Создать layout страницы AnalyzeMe: зона загрузки файла, область результатов (пустая). Tailwind + i18n ключи. | Определить структуру страницы до написания логики. | На `/analyze-me` видна страница с drag-and-drop зоной и пустой областью результатов. | Done |

---

## Итерация 2 — Полностью локальный анализ (DuckDB-Wasm)

| Итерация | Задача | Цель | Ожидаемый результат | Статус |
|---|---|---|---|---|
| 2.1 | Реализовать загрузку CSV в DuckDB-Wasm на клиенте. Файл не отправляется на сервер. | Подтвердить архитектурный принцип: весь анализ происходит в браузере. Бэкенд не задействован. | Файл загружается в DuckDB, в консоли видно количество строк. В DevTools — ноль сетевых запросов с данными файла. | Done |
| 2.2 | Реализовать SQL-детекцию nulls и duplicates через DuckDB-Wasm. | Базовые проверки целостности данных без бэкенда. | После загрузки CSV система находит null-значения и дубликаты, возвращает row_index и column. | Done |
| 2.3 | Реализовать IQR outlier detection через SQL (`PERCENTILE_CONT`) в DuckDB-Wasm. | Статистический анализ числовых колонок полностью на клиенте. | Выбросы находятся корректно по методу IQR. Для каждого выброса возвращается row_index, column, значение, границы IQR. | Done |
| 2.4 | Объединить все три детектора в единый `analyzeLocally(tableName)` метод в `shared/analytics/`. | Единая точка входа для анализа — переиспользуемая в будущих модулях. | Один вызов возвращает полный список аномалий всех типов. Покрыть unit-тестами в браузере (Vitest). | Done |

---

## Итерация 3 — UI результатов

| Итерация | Задача | Цель | Ожидаемый результат | Статус |
|---|---|---|---|---|
| 3.1 | Отобразить summary после анализа: total аномалий, разбивка по типам (nulls / duplicates / outliers). | Принцип UI "от общего к частному" — сначала сводка. | После загрузки файла появляется сводная панель с количеством аномалий каждого типа. | Not Started |
| 3.2 | Отобразить таблицу данных через `AnomalyTable` с подсветкой аномальных строк. Фильтры All / Anomalies only / по типу. | Детальный вид аномалий — пользователь видит конкретные строки. | Таблица с подсветкой работает, фильтры переключаются через DuckDB SQL-запросы. | Done |
| 3.3 | Добавить карточки аномалий под таблицей: row, column, тип, значение, описание почему это аномалия. | Объяснимость — пользователь понимает почему строка помечена. Никаких чёрных ящиков. | Каждая аномалия описана человекочитаемым текстом с указанием метода (IQR границы, дубль строки N, пустое поле). | Done |
| 3.4 | Добавить плашку "stays in your browser" рядом с зоной загрузки. Убедиться что она видна до загрузки файла. | Доверие через прозрачность — пользователь видит гарантию до того как загружает данные. | Плашка видна, кликабельна (тултип с объяснением что DuckDB-Wasm работает локально). | Done |

---

## Итерация 4 — Качество и edge cases

| Итерация | Задача | Цель | Ожидаемый результат | Статус |
|---|---|---|---|---|
| 4.1 | Обработка больших файлов: прогресс-бар загрузки, лимит размера (предупреждение при >10MB). | UX для реальных датасетов которые могут быть большими. | Файлы до 50MB загружаются без зависания UI. При >10MB — предупреждение о возможной задержке. | Done |
| 4.2 | Обработка edge cases: пустой файл, файл без заголовков, не-CSV, кодировка не UTF-8. | Инструмент не должен падать на плохих данных — должен объяснить что не так. | Каждый edge case показывает понятное сообщение об ошибке. | Done |
| 4.3 | Поддержка JSON файлов как альтернативы CSV. | Расширить применимость — многие data engineers работают с JSON. | Drag-and-drop принимает `.json` файлы, анализ работает аналогично CSV. | Not Started |
| 4.4 | Unit-тесты для всех детекторов: nulls, duplicates, IQR outliers. Vitest + тестовые CSV файлы. | Надёжность — детекторы должны быть покрыты тестами как anomaly_engine.py в ForgeMe. | Все тесты проходят. Покрытие детекторов 100%. | Not Started |

---

## Итерация 5 — i18n и финальный дизайн

| Итерация | Задача                                                                                                         | Цель | Ожидаемый результат | Статус |
|---|----------------------------------------------------------------------------------------------------------------|---|---|---|
| 5.1 | Добавить i18n ключи для AnalyzeMe в `locales/en                                                                |es|uk/common.json`. Все тексты через `t()`. | Соответствие code rules: никаких хардкоженных строк. | Переключение языка работает на странице AnalyzeMe. | Done |
| 5.2 | Финальный дизайн: привести страницу к единому стилю с ForgeMe. Tailwind + Shadcn/ui Nova preset.               | Визуальная целостность платформы. | AnalyzeMe выглядит как часть одного продукта с ForgeMe. | Done |
| 5.3 | Написать `doc/specs/analyze-me-final.md` — обновить spec с учётом всех решений принятых в процессе разработки. | Документация отражает реальность, не первоначальный план. | Spec актуален и полон. | Done |

---

## Будущее (после основной разработки)

- Schema-aware анализ: пользователь описывает ожидаемые типы колонок, система проверяет соответствие
- Экспорт отчёта об аномалиях (PDF / JSON)
- История анализов в личном кабинете
- Поддержка Parquet, Excel
- Сравнение двух датасетов (до/после очистки)

---

*Создан: май 2026. Обновлять по мере разработки.*