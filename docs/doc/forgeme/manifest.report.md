# ForgeMe · Level 1 — The Latecomer
### Test Report

| | |
|---|---|
| **Scenario** | Out-of-order arrival |
| **Seed** | `forge-42a8c1` |
| **Generated** | 2026-04-19 11:42 UTC |
| **Dataset** | 1,000 events · JSON · 10:00–10:05 UTC |

---

## What was injected

87 out of 1,000 events (8.7%) had their timestamps shuffled.
Each affected event was delayed by 1–5 seconds relative to its natural arrival order.
The rest of the dataset is clean and schema-valid.

**Injection is deterministic** — running with seed `forge-42a8c1` always produces
the same 87 events with the same delays. Share this seed to reproduce the test
on any machine or at any time.

### Sample of injected anomalies

| Event ID | Original position | Arrived at position | Delay injected |
|---|---|---|---|
| evt_0012 | 12 | 47 | 3.2 sec |
| evt_0045 | 45 | 91 | 4.7 sec |
| evt_0103 | 103 | 134 | 1.9 sec |
| evt_0217 | 217 | 249 | 2.6 sec |
| evt_0341 | 341 | 388 | 4.1 sec |
| +82 more | — | — | 1.0–5.0 sec |

Full list of affected event IDs: see `manifest.json → affected_events`

---

## What to check

These are the three things your pipeline must handle correctly at this level.
Each maps to one star in ForgeMe's progress tracker.

### ★ Check 1 — No events dropped

**What to verify:**
Count the events in your output. It must equal 1,000.

```
SELECT COUNT(*) FROM output_table
-- Expected: 1000
```

**Why it matters:**
Some systems silently drop late-arriving events instead of buffering them.
If your count is less than 1,000, your pipeline is losing data without any error log.

**Common failure:** Kafka consumer with `auto.offset.reset=latest` discards events
that arrive after the partition has moved forward.

---

### ★ Check 2 — Events reordered correctly

**What to verify:**
In your output, events must be sorted by `event_time` ascending — not by `arrival_time`.

```
SELECT event_id, event_time, arrival_time
FROM output_table
ORDER BY event_time ASC
-- Verify: event_time order matches expected sequence
```

**Why it matters:**
If your system sorts by `arrival_time` instead of `event_time`, the 87 injected events
will appear in the wrong position. Any time-windowed aggregation will produce
wrong results — with no error, no alert, just quietly wrong numbers.

**Common failure:** Flink job with processing-time semantics instead of event-time.
The job runs fine, the dashboard looks fine. The numbers are wrong.

---

### ★ Check 3 — Buffer held the full delay window

**What to verify:**
None of the 87 delayed events should appear in a late-data side output or DLQ.
All delays are within the 1–5 sec window — a correctly configured buffer must absorb them.

```
SELECT COUNT(*) FROM dead_letter_queue
WHERE run_id = 'forge-42a8c1'
-- Expected: 0
```

**Why it matters:**
This check confirms your watermark and buffer are configured wide enough
for real-world minor delays. If events are landing in DLQ at this level,
they will definitely be lost at Level 2 (Ghost of the Past), where delays reach hours.

**Common failure:** Watermark set to 0 or 1 sec for performance reasons.
Passes unit tests, fails on real traffic.

---

## How to replay this test

Same dataset, same anomalies, any time:

```bash
forgeme run --seed=forge-42a8c1 --level=1
```

Use this after fixing a bug to confirm the regression is closed.
Paste this command in your PR description so reviewers can reproduce the test.

---

## What comes next

**Level 2 — Ghost of the Past**

The same idea, but the delay is no longer 5 seconds — it's hours or days.
Your buffer won't save you. Your watermark will close. The event will arrive after
the window is gone.

The system won't crash. It will just silently drop the event and serve you
slightly wrong analytics forever.

> Run Level 1 clean before moving on. One failed check here means
> Level 2 results will be meaningless.

---

*Generated by ForgeMe · forgeme.io · No data was collected or transmitted.*